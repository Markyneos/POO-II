using System;

namespace lisex2;

class Program
{
    static void Main(string[] args)
    {
        Produto p1 = new Produto();
        Produto p2 = new();
        Produto p3 = new();
        Produto PrincipalP = new();
        Ingrediente i1 = new();
        Ingrediente i2 = new();
        Ingrediente i3 = new();
        Ingrediente PrincipalI = new();

        PrincipalI.produtos.Append(p1);
        PrincipalI.produtos.Append(p2);
        PrincipalI.produtos.Append(p3);
        PrincipalP.ingredientes.Append(i1);
        PrincipalP.ingredientes.Append(i2);
        PrincipalP.ingredientes.Append(i3);

        foreach (Produto p in PrincipalI.produtos)
        {
            Console.WriteLine(p);
        }
        foreach (Ingrediente i in PrincipalP.ingredientes)
        {
            Console.WriteLine(i);
        }
    }
}
